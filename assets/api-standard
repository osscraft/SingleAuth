API 接口规范

1. 最外层一定是Object对象
2. Object对象有三个属性：
	2.1 rsp: 只可以是0或1，0为有错误，1为正确
	2.2 code: 非负整数值，错误码，0为没有错误
	2.3 data: 内容，如果错误则为错误描述，如果正确则为标量或一个Object对象
		2.3.1 如果内容中要存储数组，使用如下对象：
			{
				list: 数组,
				total: 总数,
				since: 组合游标（单一游标、或起始位置及游标的组合、等等）
			}
		2.3.2 内容中不允许有null值，如果要为空，使用空对象：
			{}
		2.3.3 同一对象一定要结构相同
		2.3.4 拥有关联属性，尽可能增加一个关联对象，如：
			{
				id: 1,
				uid: 关联属性,
				user : { //关联对象

				}
			}
		2.3.5 类型要明确定义，
		2.4.6 对象初始值定义为stdClass类型